% Calculate nth Fibonacci number

func fib(val n) is 
  var x;
  var y;
  var z;
  var i;
{ x := 0 
; y := 1
; i := n
; while(i > 0) do 
  { z := x + y
  ; x := y
  ; y := z
  ; i := i - 1
  }
; return z
}

proc main() is
{ fib(10)
; fib(17)
}
