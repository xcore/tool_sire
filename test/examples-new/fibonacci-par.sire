% Recursively calculate the nth Fibonacci number in parallel

func fibonacci(val n, var result) is
  var n1;
  var n2;
{ if n > 1 
  then 
  { { fibonacci(n-1, n1) || fibonacci(n-2, n2) }
  ; result := n1 + n2
  }
  else if n = 0 
  then restult := 0
  else if n = 1
  then result := 1
  else skip
}

proc main() is
  var result;
{ fibonacci(10, result) 
; printvalln(result)
}
