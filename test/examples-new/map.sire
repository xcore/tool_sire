val N := 64;
val B := 4;

func f(var S[n], val n) is
  var i;
  for i:=0 to n-1 do 
    S[n] := S[n] * 2

proc main() is
  var A[N*B];
{
    par i:=0 for N do
    %{ f(!A[i*B:(i+1)*B], B)
    { f(block(A, B), B)
    }
}
