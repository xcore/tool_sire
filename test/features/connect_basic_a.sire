val N := NUM_CORES-1;

proc foo() is
  chanend c;
{ connect c:0 to 0;
  c ! 0xDEADBEEF
}

proc bar(var v) is
  chanend c;
{ connect c:0;
  c ? v
}

proc main() is
  var v;
{ { on N do foo() || bar(v) }
; printhexln(v)
}

