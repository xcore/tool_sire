% Test that we can use all the resources again

proc main() is
  var v;
  var i;
  var b;
{ b := true
; for i in [1 for 10] do
  { v := 0
  ; { on NUM_CORES-1 do v := v or 0xDEAD0000
    ; on NUM_CORES-1 do v := v or 0x0000BEEF
    }
  ; b := (v = 0xDEADBEEF) and b
  }
; if b then printhexln(v)
  else skip
}

