% Ackermann's funciton

func ackermann(val m, val n):
{ if m=0 
  then return n+1
  else skip
  if m>0 and n=0
  then return ackermann(m-1, 1)
  else skip
  if m>0 and n>0
  then return ackermann(m-1, ackermann(m, n-1))
  else skip
}

proc main():
{ print(ackermann(3,4))
; print(ackermann(4,0)) 
}
