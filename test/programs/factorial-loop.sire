% Calculate the factorial of a value

func factorial(val x) is
  var ans;
  var y;
{ ans := 1
; y := 1
; while y <= x do
  { ans := ans * y
  ; y := y + 1
  }
; return ans
}

% Ouput: 6, 120, 5040
proc main() is
{ printvalln(factorial(3))
; printvalln(factorial(5))
; printvalln(factorial(7))
}
